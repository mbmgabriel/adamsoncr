# Installation
Create `.env` file
```bash
  cp .env.sample .env
```


Edit `.env` file and provide required credentials
```bash
  nano .env`
```

Install packages
```bash
npm install
```

Start the server
```bash
npm start
```